<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config para Ephemeral Chat.
  Permite conexões HTTP (cleartext) com o servidor na rede local.
  Em produção, use HTTPS e restrinja os domínios permitidos.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Permitir tráfego cleartext para rede local e emulador -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0/16</domain>
        <domain includeSubdomains="true">10.0.0.0/8</domain>
    </domain-config>
</network-security-config>
